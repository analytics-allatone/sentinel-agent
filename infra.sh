#!/bin/bash

# Check docker exist ? 
#if not then install

# newgrp user command

# docker run postgres --params
# postgres extension + user creation and database creations command

# mqtt setup
# docker run mqtt --params 

# Download  main executable 
# Docker build with binary
# Run executable in docker 


# Exposer ports -> MQTT port + 8000